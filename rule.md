统一采用下面的 Markdown 规范：

* 不允许出现: ---
* 所有独立公式都写成一行
* 行内公式：`$u_t=Du_{xx}$`
* 独立公式：`$$...$$`,始终写成单行形式
* 不使用 `[]` 包裹公式
* 章节使用 `#`、`##`、`###`
* 重要说明使用 `> **注意：**`
* 数学公式尽量单独成行，这样 Typora、Obsidian、GitHub Pages 等都能正确渲染。
